import React from 'react';
import {View,Text, TextInput, Button } from 'react-native';

const Profile = props => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
};

export default Profile;